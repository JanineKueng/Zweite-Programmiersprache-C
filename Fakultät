#include<stdio.h>
int fakultaet(int x) {
	if(x > 1) {
		return x * fakultaet(x-1);
	}else {
		return 1;
	}
}

int main() {
    int s;
    printf("Bitte eine Zahl eingeben: \n");
    scanf("%d", &s);
    printf("Fakultaet von %d ist %d\n", s, fakultaet(s));
    return 0;
}
